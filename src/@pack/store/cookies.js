// import AsyncStorage from '@react-native-community/async-storage'

export default {
	set: console.log, //AsyncStorage.setItem,
	get: console.log, //AsyncStorage.getItem,
	clear: console.log, //AsyncStorage.clear
}
